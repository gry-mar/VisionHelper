<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    android:orientation="vertical"
    tools:context=".zoomview.ZoomViewActivity">

    <ImageButton
        android:id="@+id/btnHelperZoomView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="left"
        android:padding="16sp"
        android:background="@color/transparent"
        android:src="@drawable/ic_helper" />

    <TextView
        android:id="@+id/tvTextZoomView"
        android:layout_width="wrap_content"
        android:layout_marginLeft="30dp"
        android:layout_marginRight="30dp"
        android:layout_height="250dp"
        android:layout_marginTop="10dp"
        android:gravity="center"
        android:layout_weight='0.2'
        android:layout_gravity="center"
        android:text="Text will appear here BLE BLE BLE BLE BLE BLE BLEBLE BLE BLE BLE BLE BLE BLEBLE BLE BLE BLE BLE BLE BLEBLE BLE BLE BLE BLE BLE BLE BLE BLE BLE BLE BLE BLE BLE"
        android:textColor="@color/white" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_weight="0.1"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="horizontal"
        >
        <ImageButton
            android:id="@+id/btnMakeTextSizeSmallerZoomView"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/ic_remove"
            android:background="@color/transparent"
            />
        <Space
            android:layout_width="72sp"
            android:layout_height="match_parent"/>
        <ImageButton
            android:id="@+id/btnMakeTextSizeLargerZoomView"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/ic_add"
            android:background="@color/transparent"
            />

    </LinearLayout>

    <ImageButton
        android:id="@+id/btnConfirmTextSizeZoomView"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:src="@drawable/ic_check"
        android:layout_weight="0.1"
        android:layout_gravity="center"
        android:background="@color/transparent"
        />

</LinearLayout>